namespace BookStore.Api.Constants
{
    // This static class stores fixed token strings for authorization simulation.
   
    public static class UserRoles
    {
        public const string Fake = "fake-token";   // Represents a fake/test user
        public const string Normal = "normal-token"; // Represents a normal/real user
    }
}